/*
 * SwiftTender — main.c
 * Entry point. Initialises mongoose HTTP server and event loop.
 * Full implementation added in: feat: add mongoose.c HTTP server skeleton
 */
#include <stdio.h>

int main(void) {
    printf("SwiftTender backend — starting...\n");
    return 0;
}
